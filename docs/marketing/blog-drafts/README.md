# Blog drafts (not live)

These TypeScript drafts are **content inventory** for a future `/blog` — they are not imported by the Next.js app yet.

See `docs/marketing-plan.md` for positioning, shipped SEO pages, and the publish checklist.

When building the blog: move or adapt `posts.draft.ts` into `src/lib/blog/` (or convert to Markdown) and wire `/blog` + sitemap + public proxy.
